/**
 * The simple template has no call-to-action band: the third box ("Your
 * event") is the call to action, and repeating it below would undo the point
 * of the page. LandingPage requires a CTA component, so this renders nothing.
 */
export function CTA() {
  return null;
}
