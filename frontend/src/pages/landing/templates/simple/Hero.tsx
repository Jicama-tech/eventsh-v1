/**
 * One headline, one line. The proof is the three boxes underneath — this
 * only has to say what the thing is and point down.
 */
export function Hero() {
  return (
    <header className="wrap sp-hero">
      <h1>
        Run your whole event on <em>one link</em>.
      </h1>
      <p>Have a look at a real one, then make your own. Free to start.</p>
    </header>
  );
}
